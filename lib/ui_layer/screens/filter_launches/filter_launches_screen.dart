import 'package:flutter/material.dart';

class FilterLaunchesScreen extends StatelessWidget {
  const FilterLaunchesScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Center(child: Text('3'),),
      ],
    );
  }
}
